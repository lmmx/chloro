COMPARISON DIFF
============================================================

Original size: 30351 bytes
Chloro size:   30348 bytes
Rustfmt size:  30351 bytes

✗ Outputs DIFFER

--- DIFF (- rustfmt, + chloro) ---
 use expect_test::expect;
 use test_utils::{bench, bench_fixture, skip_slow_tests};
 
 use crate::tests::{check_infer_with_mismatches, check_no_mismatches};
-
 use super::{check_infer, check_types};
-
 #[test]
 fn cfg_impl_def() {
     check_types(
 }
     "#;
     let fixture = format!("{fixture}\n//- /foo.rs\n{data}");
-
     {
         let _b = bench("include macro");
         check_types(&fixture);