COMPARISON DIFF
============================================================

Original size: 13520 bytes
Chloro size:   13517 bytes
Rustfmt size:  13520 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
     Super(u8),
     Crate,
     /// Absolute path (::foo)
-    Abs,
-    // FIXME: Can we remove this somehow?
+    Abs, // FIXME: Can we remove this somehow?
     /// `$crate` from macro expansion
     DollarCrate(Crate),
 }
             _ => None,
         }
     }
+
     pub fn display_verbatim<'a>(
         &'a self,
         db: &'a dyn crate::db::ExpandDatabase,