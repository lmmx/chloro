COMPARISON DIFF
============================================================

Original size: 2269 bytes
Chloro size:   2261 bytes
Rustfmt size:  2269 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 //! Syntax highlighting for format macro strings.
+
 use ide_db::{
-    defs::Definition,
-    syntax_helpers::format_string::{is_format_string, lex_format_specifiers, FormatSpecifier},
-    SymbolKind,
+    defs::Definition, is_format_string, lex_format_specifiers},
+    syntax_helpers::format_string::{FormatSpecifier, SymbolKind,
 };
 use span::Edition;
-use syntax::{ast, AstToken};
+use syntax::{AstToken, ast};
 
 use crate::{
-    syntax_highlighting::{highlight::highlight_def, highlights::Highlights},
-    HlRange, HlTag,
+    highlights::Highlights}, syntax_highlighting::{highlight::highlight_def, HlRange, HlTag,
 };
 
 pub(super) fn highlight_format_string(
 
         return;
     }
-
     if let Some(parts) = sema.as_format_args_parts(string) {
         parts.into_iter().for_each(|(range, res)| {
             if let Some(res) = res {