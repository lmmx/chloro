COMPARISON DIFF
============================================================

Original size: 36173 bytes
Chloro size:   36174 bytes
Rustfmt size:  36173 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 //! Tests specific to declarative macros, aka macros by example. This covers
 //! both stable `macro_rules!` macros as well as unstable `macro` macros.
 
-mod matching;
-mod meta_syntax;
-mod metavar_expr;
-mod regression;
-mod tt_conversion;
-
 use expect_test::expect;
 
 use crate::macro_expansion_tests::check;
 
+mod matching;
+
+mod meta_syntax;
+
+mod metavar_expr;
+
+mod regression;
+
+mod tt_conversion;
+
 #[test]
 fn token_mapping_smoke_test() {
     check(
 }
 "#]],
     );
-
     check(
         r#"
 macro_rules! m {
 #[test]
 fn test_parse_macro_def_rules() {
     cov_mark::check!(parse_macro_def_rules);
-
     check(
         r#"
 macro m {
 const VALUE: u8 = 0;
 "#]],
     );
-
     check(
         r#"
 macro_rules! m {