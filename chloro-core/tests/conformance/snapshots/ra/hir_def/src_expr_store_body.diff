COMPARISON DIFF
============================================================

Original size: 4719 bytes
Chloro size:   4720 bytes
Rustfmt size:  4752 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 //! Defines `Body`: a lowered representation of functions, statics and
 //! consts.
+
 use std::ops;
 
 use hir_expand::{InFile, Lookup};
 use triomphe::Arc;
 
 use crate::{
+    DefWithBodyId, HasModule,
     db::DefDatabase,
     expr_store::{
-        lower::lower_body, pretty, ExpressionStore, ExpressionStoreSourceMap, SelfParamPtr,
+        ExpressionStore, ExpressionStoreSourceMap, SelfParamPtr, lower::lower_body, pretty,
     },
     hir::{BindingId, ExprId, PatId},
     src::HasSource,
-    DefWithBodyId, HasModule,
 };
 
 /// The body of an item (function, const etc.).
         let mut params = None;
 
         let mut is_async_fn = false;
-        let InFile {
-            file_id,
-            value: body,
-        } = {
+        let InFile { file_id, value: body } = {
             match def {
                 DefWithBodyId::FunctionId(f) => {
                     let f = f.lookup(db);