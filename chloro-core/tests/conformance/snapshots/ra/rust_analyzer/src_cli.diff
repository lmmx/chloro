COMPARISON DIFF
============================================================

Original size: 2201 bytes
Chloro size:   2168 bytes
Rustfmt size:  2201 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 mod ssr;
 mod symbols;
 mod unresolved_references;
-
 mod progress_report;
 
 use std::io::Read;
     pub fn is_verbose(self) -> bool {
         matches!(self, Verbosity::Verbose | Verbosity::Spammy)
     }
+
     pub fn is_spammy(self) -> bool {
         matches!(self, Verbosity::Spammy)
     }
 }
 
 fn full_name_of_item(db: &dyn HirDatabase, module: Module, name: Name) -> String {
-    module
-        .path_to_root(db)
-        .into_iter()
-        .rev()
-        .filter_map(|it| it.name(db))
-        .chain(Some(name))
-        .map(|it| it.display(db, Edition::LATEST).to_string())
-        .join("::")
+    module.path_to_root(db).into_iter().rev().filter_map(|it| it.name(db)).chain(Some(name)).map(
+        |it| it.display(db, Edition::LATEST).to_string(),
+    ).join(
+        "::",
+    )
 }