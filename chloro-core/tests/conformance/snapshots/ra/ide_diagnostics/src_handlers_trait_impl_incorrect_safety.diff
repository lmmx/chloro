COMPARISON DIFF
============================================================

Original size: 3077 bytes
Chloro size:   3061 bytes
Rustfmt size:  3122 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
     Diagnostic::new(
         DiagnosticCode::Ra("trait-impl-incorrect-safety", Severity::Error),
         if d.should_be_safe {
-            "unsafe impl for safe trait"
-        } else {
-            "impl for unsafe trait needs to be unsafe"
-        },
+        "unsafe impl for safe trait"
+    } else {
+        "impl for unsafe trait needs to be unsafe"
+    },
         adjusted_display_range::<ast::Impl>(
-            ctx,
-            InFile {
-                file_id: d.file_id,
-                value: d.impl_,
-            },
-            &|impl_| {
+        ctx,
+        InFile {
+        file_id: d.file_id,
+        value: d.impl_,
+    },
+        &|impl_| {
                 if d.should_be_safe {
                     Some(match (impl_.unsafe_token(), impl_.impl_token()) {
                         (None, None) => return None,
                     impl_.impl_token().map(|t| t.text_range())
                 }
             },
-        ),
+    ),
     )
     .stable()
 }
 #[cfg(test)]
 mod tests {
     use crate::tests::check_diagnostics;
-
     #[test]
     fn simple() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn drop_may_dangle() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn negative() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn inherent() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn unsafe_unresolved_trait() {
         check_diagnostics(