COMPARISON DIFF
============================================================

Original size: 3698 bytes
Chloro size:   3691 bytes
Rustfmt size:  3698 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 #[cfg(test)]
 mod tests {
     use crate::tests::check_diagnostics;
-
     #[test]
     fn option_nonexhaustive() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn option_exhaustive() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn option_nonexhaustive_inside_blocks() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn min_exhaustive() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn empty_patterns_normalize() {
         check_diagnostics(
         "#,
         );
     }
-
     #[test]
     fn regression_20259() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn uninhabited_variants() {
         check_diagnostics(