COMPARISON DIFF
============================================================

Original size: 3138 bytes
Chloro size:   3001 bytes
Rustfmt size:  3138 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 use hir::InFile;
 
 use crate::{Diagnostic, DiagnosticCode, DiagnosticsContext};
 
-// Diagnostic: trait-impl-orphan
-//
-// Only traits defined in the current crate can be implemented for arbitrary types
 pub(crate) fn trait_impl_orphan(
     ctx: &DiagnosticsContext<'_>,
     d: &hir::TraitImplOrphan,
     Diagnostic::new_with_syntax_node_ptr(
         ctx,
         DiagnosticCode::RustcHardError("E0117"),
-        "only traits defined in the current crate can be implemented for arbitrary types"
-            .to_owned(),
+        "only traits defined in the current crate can be implemented for arbitrary types".to_owned(),
         InFile::new(d.file_id, d.impl_.into()),
     )
 }
 #[cfg(test)]
 mod tests {
     use crate::tests::check_diagnostics;
-
     #[test]
     fn simple() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn generics() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn fundamental() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn dyn_object() {
         check_diagnostics(
 "#,
         );
     }
-
     #[test]
     fn twice_fundamental() {
         check_diagnostics(