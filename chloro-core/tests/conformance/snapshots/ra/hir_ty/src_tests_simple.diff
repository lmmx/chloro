COMPARISON DIFF
============================================================

Original size: 88820 bytes
Chloro size:   88721 bytes
Rustfmt size:  88820 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 }
 
 #[test]
-// FIXME
 #[should_panic]
 fn const_eval_in_function_signature() {
     check_types(
 
 #[test]
 fn async_fn_and_try_operator() {
-    check_no_mismatches(
-        r#"
+    check_no_mismatches(r#"
 //- minicore: future, result, fn, try, from
 async fn foo() -> Result<(), ()> {
     Ok(())
     let x = foo().await?;
     Ok(x)
 }
-        "#,
-    )
+        "#)
 }
 
 #[test]
 
 #[test]
 fn capture_kinds_simple() {
-    check_types(
-        r#"
+    check_types(r#"
 struct S;
 
 impl S {
     let c3 = || { let t = x; t };
       //^^ impl FnOnce() -> S
 }
-    "#,
-    )
+    "#)
 }
 
 #[test]
 
 #[test]
 fn capture_kinds_with_copy_types() {
-    check_types(
-        r#"
+    check_types(r#"
 //- minicore: copy, clone, derive
 #[derive(Clone, Copy)]
 struct Copy;
     let c3 = || e;
       //^^ impl FnOnce() -> AssocGeneric<Copy>
 }
-    "#,
-    )
+    "#)
 }
 
 #[test]
 fn closure_kind_with_predicates() {
-    check_types(
-        r#"
+    check_types(r#"
 //- minicore: fn
 #![feature(unboxed_closures)]
 
     f2(|| { 0 });
     // ^^^^^^^^ impl FnOnce() -> i32
 }
-    "#,
-    )
+    "#)
 }
 
 #[test]
 fn derive_macro_should_work_for_associated_type() {
-    check_types(
-        r#"
+    check_types(r#"
 //- minicore: copy, clone, derive
 #[derive(Clone)]
 struct X;
     let e_clone = e.clone();
       //^^^^^^^ AssocGeneric<X>
 }
-    "#,
-    )
+    "#)
 }
 
 #[test]
 fn cfgd_out_assoc_items() {
-    check_types(
-        r#"
+    check_types(r#"
 struct S;
 
 impl S {
     S::C;
   //^^^^ {unknown}
 }
-    "#,
-    )
+    "#)
 }
 
 #[test]