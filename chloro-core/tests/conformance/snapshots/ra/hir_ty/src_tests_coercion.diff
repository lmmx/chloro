COMPARISON DIFF
============================================================

Original size: 17780 bytes
Chloro size:   17463 bytes
Rustfmt size:  17780 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
 
 #[test]
 fn if_else_coerce() {
-    check_no_mismatches(
-        r#"
+    check_no_mismatches(r#"
 //- minicore: coerce_unsized
 fn foo<T>(x: &[T]) -> &[T] { x }
 fn test() {
         foo(&[1])
     };
 }
-"#,
-    )
+"#)
 }
 
 #[test]
     );
 }
 
-// FIXME(next-solver): We could learn more from the `&S` -> `&dyn Foo<i8, _>` coercion if we followed the rustc model
-// where unsized is successful if all unsizing trait goals are certain (and non-unsizing goals are delayed).
 #[test]
 fn coerce_unsize_trait_object_simple() {
     check_types(
 
 #[test]
 fn two_closures_lub() {
-    check_types(
-        r#"
+    check_types(r#"
 fn foo(c: i32) {
     let add = |a: i32, b: i32| a + b;
             //^^^^^^^^^^^^^^^^^^^^^^ impl Fn(i32, i32) -> i32
     if c > 42 { add } else { sub };
   //^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ fn(i32, i32) -> i32
 }
-        "#,
-    )
+        "#)
 }
 
 #[test]
 fn match_diverging_branch_1() {
-    check_types(
-        r#"
+    check_types(r#"
 enum Result<T> { Ok(T), Err }
 fn parse<T>() -> T { loop {} }
 
     a
   //^ i32
 }
-        "#,
-    )
+        "#)
 }
 
 #[test]
 fn match_diverging_branch_2() {
     // same as 1 except for order of branches
-    check_types(
-        r#"
+    check_types(r#"
 enum Result<T> { Ok(T), Err }
 fn parse<T>() -> T { loop {} }
 
     a
   //^ i32
 }
-        "#,
-    )
+        "#)
 }
 
 #[test]
 
 #[test]
 fn coerce_type_var() {
-    check_types(
-        r#"
+    check_types(r#"
 //- minicore: from, coerce_unsized
 fn test() {
     let x = ();
     let _: &() = &x.into();
 }               //^^^^^^^^ ()
-"#,
-    )
+"#)
 }
 
 #[test]
 
 #[test]
 fn regression_14443_dyn_coercion_block_impls() {
-    check_no_mismatches(
-        r#"
+    check_no_mismatches(r#"
 //- minicore: coerce_unsized
 trait T {}
 
     dyn_t(&a);
     dyn_t(&b);
 }
-"#,
-    )
+"#)
 }
 
 #[test]