COMPARISON DIFF
============================================================

Original size: 6079 bytes
Chloro size:   5875 bytes
Rustfmt size:  6079 bytes

✗ Outputs DIFFER

=== DIFF (- rustfmt, + chloro) ===
     AssistContext, AssistId, Assists,
 };
 
-// Assist: desugar_doc_comment
-//
-// Desugars doc-comments to the attribute form.
-//
-// ```
-// /// Multi-line$0
-// /// comment
-// ```
-// ->
-// ```
-// #[doc = r"Multi-line
-// comment"]
-// ```
 pub(crate) fn desugar_doc_comment(acc: &mut Assists, ctx: &AssistContext<'_>) -> Option<()> {
     let comment = ctx.find_token_at_offset::<ast::Comment>()?;
     // Only allow doc comments
 #[cfg(test)]
 mod tests {
     use crate::tests::{check_assist, check_assist_not_applicable};
-
     use super::*;
-
     #[test]
     fn single_line() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn single_line_indented() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn multiline() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn end_of_line() {
         check_assist_not_applicable(
 "#,
         );
     }
-
     #[test]
     fn single_line_different_kinds() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn single_line_separate_chunks() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn block_comment() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn inner_doc_block() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn block_indent() {
         check_assist(
 "#,
         );
     }
-
     #[test]
     fn end_of_line_block() {
         check_assist_not_applicable(
 "#,
         );
     }
-
     #[test]
     fn regular_comment() {
         check_assist_not_applicable(desugar_doc_comment, r#"// some$0 comment"#);
         check_assist_not_applicable(desugar_doc_comment, r#"/* some$0 comment*/"#);
     }
-
     #[test]
     fn quotes_and_escapes() {
         check_assist(